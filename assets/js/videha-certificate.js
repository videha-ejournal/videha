(function(){
"use strict";
const $=id=>document.getElementById(id),authorSearch=$("authorSearch"),searchPublication=$("searchPublication"),findAuthor=$("findAuthor"),searchStatus=$("searchStatus"),searchResults=$("searchResults"),form=$("certificateForm"),authorName=$("authorName"),articleTitle=$("articleTitle"),publicationName=$("publicationName"),issueNumber=$("issueNumber"),versionField=$("versionField"),issueVersion=$("issueVersion"),certificateDate=$("certificateDate"),authorDeclaration=$("authorDeclaration"),matchSummary=$("matchSummary"),formStatus=$("formStatus"),sheet=$("certificateSheet"),emptyMessage=$("emptyMessage"),certificateContent=$("certificateContent"),printCertificate=$("printCertificate");
const GH_ROOT="https://videha-ejournal.github.io/videha/",PF_CANDIDATES=[new URL("pagefind/pagefind.js",document.baseURI).href,GH_ROOT+"pagefind/pagefind.js"],KNOWLEDGE_CANDIDATES=[new URL("data/videha-knowledge-graph.json",document.baseURI).href,GH_ROOT+"data/videha-knowledge-graph.json"];let pagefindPromise=null,knowledgePromise=null,selected=null,lastMatches=[];
const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const plain=s=>String(s||"").replace(/<[^>]+>/g," ").replace(/\s+/g," ").trim();
const deva=n=>String(n).replace(/[0-9]/g,d=>"०१२३४५६७८९"[+d]);
function setStatus(el,text,kind=""){el.textContent=text;el.className="vpc-status"+(kind?" "+kind:"")}
function updateVersion(){const show=publicationName.value==="SADEHA"&&String(issueNumber.value)==="5";versionField.hidden=!show;issueVersion.required=show;if(!show)issueVersion.value="1";clearMatchIfChanged()}
function outputFor(pub,issue,version){if(pub==="VIDEHA")return `videha-${String(issue).padStart(3,"0")}.html`;return +issue===5?`sadeha-005-version-${version||1}.html`:`sadeha-${String(issue).padStart(3,"0")}.html`}
function canonicalRecord(pub,issue,version){return GH_ROOT+"search-documents/"+outputFor(pub,issue,version)}
function parseResult(data){const url=String(data.url||""),m=url.match(/search-documents\/(videha|sadeha)-(\d+)(?:-version-([12]))?\.html/i);if(!m)return null;const pub=m[1].toUpperCase(),issue=+m[2],version=m[3]||"";return{pub,issue,version,output:m[0].split("/").pop(),url:canonicalRecord(pub,issue,version),title:plain(data.meta?.title)||`${pub} — ${issue}`,excerpt:plain(data.excerpt||data.plain_excerpt||"")}}
async function getPagefind(){if(!pagefindPromise)pagefindPromise=(async()=>{let error;for(const url of PF_CANDIDATES){try{const pf=await import(url);await pf.init();return pf}catch(e){error=e}}throw error||new Error("Pagefind unavailable")})();return pagefindPromise}
async function getKnowledge(){if(!knowledgePromise)knowledgePromise=(async()=>{let error;for(const url of KNOWLEDGE_CANDIDATES){try{const response=await fetch(url,{cache:"no-cache"});if(!response.ok)throw new Error(`HTTP ${response.status}`);return response.json()}catch(e){error=e}}throw error||new Error("Author index unavailable")})();return knowledgePromise}
const norm=s=>String(s||"").normalize("NFKC").toLocaleLowerCase().replace(/[\u200b-\u200d\ufeff]/g,"").replace(/\s+/g," ").trim();
async function knowledgeMatches(query,scope){if(scope==="SADEHA")return[];const data=await getKnowledge(),needle=norm(query),out=[];for(const author of data.authors||[]){if(!norm(author.name).includes(needle))continue;for(const issue of author.issues||[]){out.push({pub:"VIDEHA",issue:+issue,version:"",output:outputFor("VIDEHA",issue,""),url:canonicalRecord("VIDEHA",issue,""),title:`VIDEHA — Issue ${issue} / अंक ${deva(issue)}`,excerpt:`Indexed author: ${author.name}${author.examples?.[0]?" · "+author.examples[0]:""}`,authorLabel:author.name,indexed:true})}}return out.slice(0,160)}
async function pagefindMatches(query,scope){const pf=await getPagefind(),opts=scope?{filters:{publication:scope}}:{},found=await pf.search(query,opts),rows=await Promise.all((found.results||[]).slice(0,100).map(x=>x.data())),out=[];for(const row of rows){const item=parseResult(row);if(item)out.push(item)}return out}
async function archiveMatches(query,scope){let videha=[],sadeha=[];if(scope!=="SADEHA"){try{videha=await knowledgeMatches(query,scope)}catch(error){console.warn(error)}}if(scope!=="VIDEHA"){try{sadeha=await pagefindMatches(query,"SADEHA")}catch(error){console.warn(error)}}if(scope==="VIDEHA"&&!videha.length){try{videha=await pagefindMatches(query,"VIDEHA")}catch(error){console.warn(error)}}if(!scope&&!videha.length&&!sadeha.length){try{return pagefindMatches(query,"")}catch(error){console.warn(error)}}return videha.concat(sadeha)}
function renderMatches(matches){searchResults.innerHTML="";lastMatches=matches;for(const [index,item] of matches.entries()){const button=document.createElement("button");button.type="button";button.className="vpc-result";button.dataset.index=index;button.innerHTML=`<strong>${esc(item.title)}</strong><span class="badge">${esc(item.pub)}</span><small>${esc(item.excerpt||"Author-name match in searchable issue text")}</small>`;button.addEventListener("click",()=>selectMatch(index,button));searchResults.appendChild(button)}}
function selectMatch(index,button){const item=lastMatches[index];if(!item)return;selected=item;authorName.value=item.authorLabel||authorName.value.trim()||authorSearch.value.trim();publicationName.value=item.pub;issueNumber.value=item.issue;issueVersion.value=item.version||"1";updateVersion();searchResults.querySelectorAll(".vpc-result").forEach(x=>x.classList.remove("selected"));button.classList.add("selected");matchSummary.className="vpc-match verified";matchSummary.textContent=`अभिलेख मिलान चयनित · Archive match selected: ${item.title}`;setStatus(formStatus,"अब रचनाक शीर्षक भरि declaration स्वीकार करू। · Enter the work title and accept the declaration.","good")}
function clearMatchIfChanged(){if(!selected)return;const version=(publicationName.value==="SADEHA"&&+issueNumber.value===5)?issueVersion.value:"";if(selected.pub!==publicationName.value||selected.issue!==+issueNumber.value||(selected.version||"")!==version){selected=null;matchSummary.className="vpc-match";matchSummary.textContent="विवरण बदलल गेल; प्रमाणपत्र लेखकक स्व-घोषणा पर बनत। · Details changed; certificate will use the author’s declaration."}}
async function search(){const query=authorSearch.value.trim();if(query.length<2){setStatus(searchStatus,"कम-सँ-कम दू अक्षर लिखू · Enter at least two characters.","error");authorSearch.focus();return}findAuthor.disabled=true;searchResults.innerHTML="";setStatus(searchStatus,"VIDEHA / SADEHA अभिलेख खोजल जा रहल अछि… · Searching the archive…");try{const found=await archiveMatches(query,searchPublication.value),seen=new Set(),matches=[];for(const item of found){if(!seen.has(item.output)){seen.add(item.output);matches.push(item)}}matches.sort((a,b)=>(+b.indexed)-(+a.indexed)||b.issue-a.issue||a.pub.localeCompare(b.pub));renderMatches(matches);authorName.value=authorName.value||query;if(matches.length)setStatus(searchStatus,`${matches.length} matching issue${matches.length===1?"":"s"} भेटल। सही अंक चुनू · Select the correct publication issue.`,"good");else setStatus(searchStatus,"अभिलेखमे सीधा मिलान नहि भेटल। वर्तनी जाँचू वा नीचे manual details भरू। · No direct match; check spelling or use manual entry.","error")}catch(error){console.error(error);setStatus(searchStatus,"Search index उपलब्ध नहि अछि। नीचे manual details भरि स्व-प्रमाणपत्र बनाओल जा सकैत अछि। · Archive search unavailable; manual self-certification remains available.","error")}finally{findAuthor.disabled=false}}
async function referenceId(parts){const raw=parts.join("|").normalize("NFKC");try{const digest=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(raw));return"VSC-"+[...new Uint8Array(digest)].slice(0,6).map(x=>x.toString(16).padStart(2,"0")).join("").toUpperCase()}catch(e){let h=2166136261;for(const ch of raw)h=Math.imul(h^ch.charCodeAt(0),16777619);return"VSC-"+(h>>>0).toString(16).toUpperCase().padStart(8,"0")}}
function formatDate(value){const d=new Date(value+"T12:00:00");return Number.isNaN(d.getTime())?value:new Intl.DateTimeFormat("en-IN",{day:"2-digit",month:"long",year:"numeric"}).format(d)}
async function generate(event){event.preventDefault();formStatus.className="vpc-status";if(!form.reportValidity())return setStatus(formStatus,"सभ आवश्यक विवरण भरू आ declaration स्वीकार करू। · Complete all required fields and accept the declaration.","error");const author=authorName.value.trim(),title=articleTitle.value.trim(),pub=publicationName.value,issue=+issueNumber.value,version=(pub==="SADEHA"&&issue===5)?issueVersion.value:"",date=certificateDate.value;if(!author||!title||!issue)return setStatus(formStatus,"लेखक, शीर्षक आ अंक आवश्यक अछि। · Author, title and issue are required.","error");const matched=!!(selected&&selected.pub===pub&&selected.issue===issue&&(selected.version||"")===version),record=matched?selected.url:canonicalRecord(pub,issue,version),id=await referenceId([author,title,pub,issue,version,date]),issueLabel=pub==="VIDEHA"?`VIDEHA — Issue ${issue} / अंक ${deva(issue)}`:`SADEHA — ${issue}${version?`, Version ${version}`:""}`;$("certAuthor").textContent=author;$("certArticle").textContent=`“${title}”`;$("certIssue").textContent=issueLabel;$("certMatch").textContent=matched?"लेखक-नाम searchable archive मे एहि अंकसँ मिलल · Author name matched to this issue in the searchable archive":"लेखक द्वारा भरल स्व-घोषित प्रकाशन विवरण · Publication details self-declared by the author";$("certDate").textContent=formatDate(date);$("certId").textContent=id;$("certSignature").textContent=author;$("certSource").href=record;$("certSource").textContent="Open publication record";sheet.classList.remove("is-empty");emptyMessage.hidden=true;certificateContent.hidden=false;printCertificate.disabled=false;setStatus(formStatus,"प्रमाणपत्र तैयार। Preview जाँचि Print · Save PDF दबाउ। · Certificate generated; review it before printing.","good");sheet.scrollIntoView({behavior:"smooth",block:"center"})}
function resetAll(){form.reset();authorSearch.value="";searchPublication.value="";searchResults.innerHTML="";selected=null;lastMatches=[];certificateDate.value=new Date().toISOString().slice(0,10);versionField.hidden=true;issueVersion.required=false;matchSummary.className="vpc-match";matchSummary.textContent="अभिलेख मिलान एखन चयनित नहि अछि · No archive match selected.";sheet.classList.add("is-empty");emptyMessage.hidden=false;certificateContent.hidden=true;printCertificate.disabled=true;setStatus(searchStatus,"नाम लिखि Search दबाउ। Manual entry is also available below.");setStatus(formStatus,"")}
certificateDate.value=new Date().toISOString().slice(0,10);findAuthor.addEventListener("click",search);authorSearch.addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();search()}});publicationName.addEventListener("change",updateVersion);issueNumber.addEventListener("input",updateVersion);issueVersion.addEventListener("change",clearMatchIfChanged);form.addEventListener("submit",generate);$("resetCertificate").addEventListener("click",resetAll);printCertificate.addEventListener("click",()=>window.print());
})();
